{% extends 'page.html.twig' %}
{% block javascripts %}
<script>
$("select").change(function() {
			$("#search").submit();						
		});
		 $("input").blur(function() {
			$("#search").submit();						
		});
</script>
{% endblock %}
{% block body %}

    <section class="section clearfix">
        <div class="container">
        <h3>Add Top Popular Destinations</h3>
            <div class="row">
            	<div id="fullwidth" class="col-sm-6">
            		{{ form_start(form) }} 
                    {{ form_end(form) }}
            	</div>
                <div id="fullwidth" class="col-sm-6">
					<table class="table table-bordered col-lg-12 col-sm-12">
					<tr>
						<th>Sl.No</th>
						<th>Url</th>
						<th></th>
						
						<th>/</th>
					</tr>
                     {% for destlist in destinationsList %}
                        <tr>
                        	<td>{{ loop.index }}</td>
							 <td>
                                {{ destlist.desturl }}
                            </td>
							 <td>
                                
                            </td>
							
							<td>
								<a href="{{ path('trip_booking_engine_edit_searchpages',{ 'id': destlist.id }) }}"><i class="fa fa-pencil-square-o"></i></a>
							</td>
                        </tr>
                    {% endfor %}
					</table>
                </div>
            </div>
        </div>
</section>
{% endblock %}